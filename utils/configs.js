export const configs = {
    ids: function () {
        return {
            title : "__TITLE",
            results : "__RESULTS",
            sliderAge: "__SLIDER_AGE",
            sliderHeight: "__SLIDER_HEIGHT"
        }
    }
}